global_costmap:
  global_frame: base_footprint
  #global_frame: gps_frame
  #global_frame: odom
  robot_base_frame: base_footprint
  #global_frame: gps_frame
  #robot_base_frame: grill_link
  update_frequency: 20.0
  publish_frequency: 20.0
  static_map: true
  #static_map: false
  #rolling_window: true
  rolling_window: false

  #width: 100
  #height: 100
  width: 1000
  height: 1000

  transform_tolerance: 0.5

  plugins:
    - {name: global_static_layer,            type: "costmap_2d::StaticLayer"}
 #   - {name: global_csv_static_layer,        type: "costmap_2d::StaticLayer"}
    - {name: global_inflation_layer,         type: "costmap_2d::InflationLayer"}
